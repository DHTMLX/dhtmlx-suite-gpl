import { Combo, IComboConfig } from "./combo";
export declare class ProCombo extends Combo {
    constructor(container: any, config: IComboConfig);
    protected _initView(config: IComboConfig): void;
}
